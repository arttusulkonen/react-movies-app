import React, {Component} from 'react';export default class Search extends Component {    state = {        title: '',    }    handleKey = (e) => {        if(e.key === 'Enter'){            this.props.search(this.state.title)        }    }    onLabelChange = (event) => {        this.setState({            title: event.target.value,        })    }    render() {        return (            <div className='row'>                <div className="input-field">                    <input                        placeholder='search'                        type="search"                        className="validate"                        value={this.state.search}                        onChange={this.onLabelChange}                        onKeyDown={this.handleKey}                    />                    <button className='btn search-btn' onClick={() => this.props.search(this.state.title)}>Search</button>                </div>            </div>        )    }}