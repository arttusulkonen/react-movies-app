import React, {useState} from 'react';const Search = (props) => {    const {        searchMovies /// устанвилваем значение по умолчанию, что бы ничего не сломалось    } = props;    const [search, setSearch] = useState('')    const [type, setType] = useState('all')    const handleKey = (event) => {        if(event.key === 'Enter'){            searchMovies(search, type)        }    }    const handleChange = (event) => {        setType(event.target.value)        searchMovies(search, event.target.type)    }            return (            <div className='row'>                <div className="input-field">                    <input                        placeholder='search'                        type="search"                        className="validate"                        value={search}                        onChange={(e) => setSearch(e.target.value)}                        onKeyDown={handleKey}                    />                    <button className='btn search-btn'                            onClick={() =>                              searchMovies(search, type)                            }>                        Search                    </button>                    <div className='radio-buttons'>                        <label>                            <input                                className="with-gap"                                name="type"                                type="radio"                                value='all'                                onChange={handleChange}                                checked={type === 'all'}                            />                            <span>All</span>                        </label>                        <label>                            <input                                className="with-gap"                                name="type"                                type="radio"                                value='movie'                                onChange={handleChange}                                checked={type === 'movie'}/>                            <span>Movie</span>                        </label>                        <label>                            <input                                className="with-gap"                                name="type"                                type="radio"                                value='series'                                onChange={handleChange}                                checked={type === 'series'}/>                            <span>Series</span>                        </label>                    </div>                </div>            </div>        )}export default Search;